# Quick start

```bash
sudo apt-get install python-dev libxml2-dev libxslt-dev zlib1g-dev python-pip python-virtualenv
virtualenv venv
source venv/bin/activate
pip install -r requirements.txt
./example.py <server-name> <cosign-cookie>
```

